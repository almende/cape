# Eve settings

services:
- class: HttpService
  servlet_url: http://openid.almende.org:8080/cape/agents/
- class: XmppService
  host: openid.almende.org
  port: 5222
  service: openid.almende.org
       
# context settings
# the context is used by agents for storing their state.
context:
  class: FileContextFactory
  path: .eveagents
